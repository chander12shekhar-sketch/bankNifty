When the user asks for Bank Nifty, BANKNIFTY, next move, CE/PE, options, or a market brief:

1. In the repo root run `npm run brief` (install with `npm install` first if needed).
2. Reply with that CLI output. Do not invent prices or strikes if the command fails.
3. Do not edit files, commit, or open a pull request unless they asked to change the desk.
4. This is research, not a trade order.
